spring:
  application:
    name: spring-ecom-app

  datasource:
    url: jdbc:postgresql://localhost:5432/Dev
    username: postgres
    password: root
    hikari:
      auto-commit: false

  jpa:
    hibernate:
      ddl-auto: update
